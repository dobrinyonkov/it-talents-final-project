(function() {
  app.controller("PhotoGalleryController", function(
    // $window,
    // $rootScope,
    // fileUpload,
    // $routeParams,
    $scope,
    // PostService,
    // UserService,
    // $timeout
  ) {
      console.log("toz kontroller bachka")
    var userId = $routeParams.id;
    console.log(userId);
    // UserService.getAndSafeLoggedUser(userId).then(r => {
    //   $scope.profile = r;
    // });
  });
});
